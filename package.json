{
  "name": "test-opti",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "start-base": "tsc && node ./dist/index.js",
    "start-worker": "tsc && node ./dist/thread.js",
    "start-gpu": "tsc && node ./dist/gpu.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Madderstone",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "gpu.js": "^2.16.0"
  }
}
